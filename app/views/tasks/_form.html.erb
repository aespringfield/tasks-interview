<%= form_for task, html: { class: "flex flex-col w-fit gap-y-4",
      data: (task.persisted? ? { turbo: false } : {}) } do |form| %>
  <%= render "errors", task: task %>

  <%= form.label :title %>
  <%= form.text_field :title %>

  <%= form.label :description %>
  <%= form.text_area :description %>

  <%= form.label :assignee_id, "Assignee" %>
  <%= form.collection_select :assignee_id, users, :id, :name, include_blank: "Unassigned" %>

  <div class="flex justify-start items-center gap-x-2">
    <%= form.submit "Save Task", class: "p-2 border rounded bg-gray-100 hover:bg-gray-200 cursor-pointer" %>
    <%- unless task.new_record? %>
      <%= link_to "Cancel", tasks_path %>
    <%- end %>
  </div>

<% end %>
